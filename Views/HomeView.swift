import SwiftUI

struct HomeView: View {
    @ObservedObject var authVM: AuthViewModel
    
    
    var body: some View {
        NavigationView {
            
            }
            .navigationBarHidden(true)
    }
}
